export const APP_CONSTANTS = {
  /** Number of days to look back for trending repos */
  DAYS_THRESHOLD: 30,
  /** Pixels from bottom to trigger infinite scroll */
  SCROLL_THRESHOLD: 200,
} as const;
